# <title>

> Parent issue. Sub-issues attach via Linear's native parent/child
> relationship (`parentId`), not a task list.

## Context

<!-- One paragraph: who does what with this feature and why it exists. Use CONTEXT.md vocabulary for every domain term — Facility, Carrier, Driver, Vehicle, Visit, gateInAt/gateOutAt, yardStay, Packet, domain event, projection. Do not invent a synonym. -->

## High-level PRD

**Problem.** <!-- The user-facing problem, and the properties the solution must hold (exactness, freshness, who trusts the record). -->

**User story.** As a <role>, I <action> so <outcome>.

**Scope.**

- <!-- Bullet per behavior in scope — API surface, UI surface. -->

**Non-goals.** <!-- What this feature deliberately excludes; name the entity or later ticket that owns each exclusion when one does. -->

**Acceptance criteria.**

<!-- Each criterion is one observable, checkable outcome. These become the verify gate's scenarios and each slice PR's checklist — write them so a verifier can mark Passed/Failed from evidence alone. -->

- [ ] <criterion>
- [ ] <criterion>
